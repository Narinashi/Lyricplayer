namespace LyricPlayer.Model
{
    public enum PlayerStatus
    {
        Stopped = 0,
        Playing = 1,
        Paused = 2,
    }

    public enum LyricPlayerStaus
    {
        Stopped = 0,
        Playing = 1,
        Paused = 2,
        Loading = 3,
    }
}
